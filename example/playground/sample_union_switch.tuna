import { log, to_string } from "prelude"

function describe(result: i64 | string): string {
  return switch (result) {
    case code as i64: "success code: " + to_string(code)
    case reason as string: "error reason: " + reason
  }
}

export function main(): void {
  log(describe(200))
  log(describe("permission denied"))
}
