// expect: true
// expect: {"x":1}

import { log } from "prelude"
import { stringify } from "json"

export function main(): void {
  const u: undefined = undefined
  log(u == undefined)

  const obj = { "name": undefined, "x": 1 }
  log(stringify(obj))
}
