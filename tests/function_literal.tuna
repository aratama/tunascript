// expect: [2,4,6,8]
// expect: [2,4]
// expect: 10
import { log } from "prelude"
import { stringify } from "json"
import { filter, map, reduce } from "array"

const numbers: i64[] = [1, 2, 3, 4]
const doubled: i64[] = map(numbers, function (value) {
  return value * 2
})
const evens: i64[] = filter(numbers, function (value) {
  return value % 2 == 0
})
const total: i64 = reduce(numbers, function (acc, value) {
  return acc + value
}, 0)

export function main(): void {
  log(stringify(doubled))
  log(stringify(evens))
  log(total)
}
