// expect: ready

import { log, type Result, type Error } from "prelude";

function describe(response: Result<string>): string {
  return switch (response) {
    case value as string: value
    case { message } as Error: message
  }
}

export function main(): void {
  log(describe("ready"));
}
