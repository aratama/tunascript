// expect: 20
// expect: index out of range

import { log, to_string } from "prelude"

function getAt(xs: i64[], i: i64): i64 | error {
  const value: i64 = xs[i]?
  return value
}

export function main(): void {
  const xs = [10, 20, 30]

  const ok = getAt(xs, 1)
  switch (ok) {
    case n as i64: {
      log(to_string(n))
    }
    case e as error: {
      log(e.message)
    }
  }

  const ng = getAt(xs, 5)
  switch (ng) {
    case n as i64: {
      log(to_string(n))
    }
    case e as error: {
      log(e.message)
    }
  }
}
